// loading.tsx — Next.js loading UI shown while the fleet page fetches data.
export default function FleetLoading() {
  return (
    <div className="flex-1 flex items-center justify-center text-zinc-600 font-mono text-sm">
      Loading fleet…
    </div>
  );
}
